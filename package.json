{
  "name": "omnipro-220-ai",
  "scripts": {
    "dev": "concurrently \"python -m uvicorn backend.main:app --reload --port 8090\" \"npm run dev --prefix frontend\"",
    "install:all": "pip install -r requirements.txt && cd frontend && npm install",
    "frontend": "cd frontend && npm run dev",
    "backend": "python -m uvicorn backend.main:app --reload --port 8090"
  },
  "dependencies": {
    "concurrently": "^9.0.0",
    "next-themes": "^0.4.6"
  }
}
